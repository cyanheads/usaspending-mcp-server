---
summary: "release:github npm script runs under bun run instead of tsx (an undeclared dependency), fixing the broken GitHub Release step"
breaking: false
security: false
---

# 0.2.2 — 2026-06-15

## Fixed

- **`release:github` script** runs `bun run scripts/release-github.ts` instead of `tsx scripts/release-github.ts` — `tsx` is not a declared dependency, so the script failed to resolve a runner.

## Dependency bumps

- `@biomejs/biome` `^2.4.16` → `^2.5.0`
- `vitest` `^4.1.8` → `^4.1.9`
